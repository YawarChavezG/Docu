# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/

# Node / Vite
node_modules/
dist/
.vite/
.tmpvenv/

# Docker
postgres_data/
redis_data/
backend_storage/
minio_data/
nginx_logs/

# Plantillas documentales (binarios grandes, se copian via docker cp en start)
# Mantenemos el directorio en git para preservar estructura + .gitkeep
backend/storage/plantillas/*
!backend/storage/plantillas/.gitkeep

# IDEs
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.swp
*.swo
.DS_Store
Thumbs.db

# Secrets
.env
.env.local
.env.*.local
.env.backup
.env.qas
*.pem
*.key
*.crt
secrets/

# Logs
*.log
logs/

# OS
.DS_Store

# ─── Dev artifacts (outputs de procesos locales, NO se commitean) ───
# Outputs del backend nativo (.bat) cuando se corre fuera de Docker
backend.err
backend.out
backend.pid
bg.err
bg.out

# Cookies de sesiones (curl + tests manuales)
cookies.txt
cookies_local.txt
cookies_*.txt

# JSONs de prueba manual (login, test_*, etc.)
login_*.json
login_*_local.json
test_*.json
test_imp_*.json
*_local.json
trash*.json

# Outputs de procesos en background (nohup, start /B, etc.)
*.pid
nohup.out
*.nohup
*.pdf
*.xlsx

# Screenshots de validacion visual (sesiones de QA / debugging)
screenshots_sb/

# Archivos de recuperacion de sesion (no son parte del proyecto)
docs/PR/RETORNO_ANTERIOR_SESION.md

# Backups locales de BD (snapshots pg_dump - grandes y efimeros)
backups/

# Scripts temporales de prueba (Python sueltos)
tmp_*.py
tmp_*.xlsx
tmp_*.docx

# Screenshots de debugging
docs/PR/screenshots/
docs/*.docx

# Test artifacts sueltos
test_served.js
test_*.js
test_*.py
